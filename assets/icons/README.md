# assets/icons

Toolbar and shape-library icons are drawn as inline SVG directly in
`index.html` and generated at runtime in `js/nodes.js` (`App.shapeThumb`),
so no external icon files are required.

Drop custom `.svg` icons here if you want to swap the toolbar glyphs —
reference them from the `<button class="tool-btn">` markup in `index.html`.
